[package]
name = "commitscape-core"
description = "Data model shared by every commitscape layer. Contains no I/O and no git."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_bytes = "0.11.19"
